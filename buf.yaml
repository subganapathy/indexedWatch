# buf.yaml - Buf configuration for IndexedWatch
# https://buf.build/docs/configuration/v2/buf-yaml
version: v2

modules:
  - path: proto
    name: buf.build/subganapathy/indexedwatch

lint:
  use:
    - STANDARD

breaking:
  use:
    - FILE
